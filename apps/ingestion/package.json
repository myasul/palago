{
  "name": "@palago/ingestion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "backfill": "tsx jobs/backfill.ts",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.621.0",
    "@palago/db": "*",
    "@palago/pse-edge": "*",
    "@palago/types": "*",
    "date-fns": "^4.1.0",
    "date-fns-tz": "^3.2.0",
    "yahoo-finance2": "^2.13.3",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^20.17.30",
    "tsx": "^4.21.0",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4"
  }
}
