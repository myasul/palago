{
  "name": "@palago/ingestion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@palago/db": "*",
    "@palago/types": "*"
  },
  "devDependencies": {
    "@types/node": "^20.17.30",
    "typescript": "^5.8.2"
  }
}
